name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

 # Package dependencies
  - numpy
  - matplotlib
 
    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov

